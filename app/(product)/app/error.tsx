'use client'
export default function AppError({ reset }: { error: Error; reset: () => void }) { return <div className="rounded-card border border-[hsl(var(--danger)/.35)] bg-[hsl(var(--danger-soft))] p-6"><h2 className="font-display text-xl font-bold text-[hsl(var(--danger))]">The application data could not be loaded.</h2><p className="mt-2 text-sm text-[hsl(var(--foreground-secondary))]">Check your connection, then try again.</p><button onClick={reset} className="mt-5 min-h-11 rounded-input bg-accent px-4 text-sm font-bold text-[hsl(var(--accent-foreground))]">Try again</button></div> }
